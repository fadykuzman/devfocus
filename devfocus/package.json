 {
    "name": "devfocus-workspace",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
      "apps/*",
      "packages/*"
    ],
    "scripts": {
      "dev:dashboard": "pnpm --filter dashboard dev",
      "build:dashboard": "pnpm --filter dashboard build",
      "test:dashboard": "pnpm --filter dashboard test",
      "lint": "pnpm --filter dashboard lint"
    },
    "devDependencies": {
      "typescript": "^5"
    },
    "engines": {
      "node": ">=18",
      "pnpm": ">=8"
    }
  }
